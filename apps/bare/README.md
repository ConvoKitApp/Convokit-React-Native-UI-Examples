# Bare React Native example

React Native 0.87 application for iOS and Android. Copy `.env.example` to
`.env`; the default showcase needs no backend. Set `CONVOKIT_EXAMPLE_MODE=live`
and the public client/token endpoint values to run the live example.

The checked-in native projects are generated with the React Native Community
CLI and use the New Architecture and Hermes.
