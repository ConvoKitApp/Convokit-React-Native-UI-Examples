# Expo example

Expo SDK 57 / React Native 0.86 application using the same UI and fixtures as
the bare example. Copy `.env.example` to `.env`. The default showcase runs
without a backend; live mode requires only a public client ID and token endpoint.
